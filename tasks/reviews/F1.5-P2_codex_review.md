# レビュー: F1.5-P2 stakeholder ノート構造レビュー

> レビュー日時: 2026-05-16
> レビュー対象commit: dca4bd1
> レビューア: Codex

## 結果サマリー
- 🟡 PASS with fixes: 構造化トークン・型整合・ビルドは問題なし。明確な軽微不整合 2 件を `NOTE_DB.stakeholder` 内で修正済み。

## 観点別評価

### 仕様適合
- 完了条件: ○ `tasks/reviews/F1.5-P2_codex_review.md` を作成し、`npm run validate-data` / `npm run build` は成功。
- 範囲遵守: ○ 修正は `src/pages/NoteDetail.tsx` の `NOTE_DB.stakeholder` 内のみ。`categories.ts` や他カテゴリは未編集。
- セクション数: ○ `sections` は 33 要素、heading は `1. ` 〜 `33. ` の連番。
- exam_tips: ○ 7 要素で非空。

### コード品質
- 型エラー: ○ `npm run build` 成功。
- ESLint: △ `npm run lint` は既存の out-of-scope エラーで失敗。F1.5-P2 DoD 対象外のため本レビューでは修正しない。
- 命名規約: ○ `NOTE_DB.stakeholder` / `NOTE_SECTION_INDEX` の既存構造を維持。

### 既存コードとの整合
- import: ○ 追加・削除なし。`NOTE_DB` export 維持。
- LocalStorageキー: ○ 変更なし。`pmap:` prefix への影響なし。
- category 整合: ○ `categories.ts` の `stakeholder` 名は「ステークホルダー」。
- スコープ: ○ `NOTE_DB` に追加済カテゴリは `stakeholder` のみ。

### 構造化トークン
- `==X==` ペア: ○ 文字列単位で片側欠落なし。
- `===` 混入: ○ stakeholder 文字列内では検出なし。
- 全角イコール: ○ 修正後は stakeholder 文字列内で検出なし。
- `==` 内改行: ○ 文字列単位で検出なし。
- `EmphasisToken.style`: ○ `red | navy | plain` のみ。
- `navyItems`: ○ 全 token が `style: 'navy'`。
- `headerDiagrams`: ○ 2 箇所（§13, §18）。全 cell の `bg` は `#` 始まりの hex 文字列。
- `headerDiagrams` label 改行: ○ §13 の英語併記セル 4 件のみで、図表内の意図的な改行。

### 表記揺れ
- ステークホルダー: ○ 「ステイクホルダー」「ステイクホルダ」なし。
- エンゲージメント: ○ 「エンゲイジメント」なし。
- サリエンスモデル: ○ 「セイリエンスモデル」なし。
- マトリクス: ○ 「マトリックス」なし。
- コミュニケーション: ○ 代表的な誤表記なし。

### 動作確認
- npm install: ○ `up to date`、脆弱性 0。
- npm run validate-data: ○ `[OK] 全データの整合性確認完了`。297 件の orphan question warning は F2-P2 までの既知暫定状態。
- npm run build: ○ 成功。Vite の chunk size warning のみ。
- npm run dev: - 指示書 Step 6 の対象外のため未実行。
- 画面表示: - 静的構造レビューのため未実行。

## 指摘事項

### 致命（修正必須）
- なし

### 改善推奨
- なし

## Codexによる追加修正
- `src/pages/NoteDetail.tsx:590`: 構造化トークン観点で全角イコール `＝` を避けるため、「権力＝公式権威、影響力＝非公式に他者を動かす力」を「権力は公式権威、影響力は非公式に他者を動かす力」に修正。
- `src/pages/NoteDetail.tsx:652`, `src/pages/NoteDetail.tsx:653`, `src/pages/NoteDetail.tsx:654`, `src/pages/NoteDetail.tsx:662`, `src/pages/NoteDetail.tsx:665`, `src/pages/NoteDetail.tsx:666`: §18 header diagram の空セルに `bg: '#ffffff'` を追加し、全 cell の `bg` を hex 文字列に統一。

## 次のタスクへの教訓
- header diagram の空セルにも `bg` を明示すると、構造レビューの機械チェックが安定する。
- `npm run lint` は現状 repo 全体の既存エラーを拾うため、F1.5-P2 のDoD判定は `validate-data` / `build` を主判定にするのが妥当。
