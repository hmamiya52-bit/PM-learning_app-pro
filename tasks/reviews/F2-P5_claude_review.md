# F2-P5 午後II 論述問題 Claude レビュー記録

> 実施: Claude（2026-05-26）
> 対象 commit:
> - `c9b5e6c` [X] 午後Ⅱ設問と解答欄を近接表示
> - `2c6f7fc` [X] F2-P5 R5-R1 午後Ⅱ問題を投入（5 年度 × 2 問 = 10 件）
> - `71386d9` [X] F2-P5 H30-H25 午後Ⅱ問題を投入（6 年度 × 2 問 = 12 件）
> - `1a953d1` [X] 午後Ⅱセルフレビュー指摘を修正
> 対象ファイル:
> - `src/data/essayProblems.ts`（762 行）
> - `src/pages/EssayTraining.tsx`（UI 改修）
> - `src/pages/EssayAttemptDetail.tsx`（ユーティリティ集約）
> - `src/components/essay/EssayCharCounter.tsx`（未着手表示修正）
> - `src/components/essay/EssaySelfReview.tsx`（defaultSelfReview 移管）
> - `src/lib/essayReview.ts`（新規・ユーティリティ集約先）

## 結論

**追加修正なし**。データ品質・UI 改修ともに良好。Codex の自己セルフレビューによる微修正（未着手表示・ユーティリティ集約）も適切。

## データレビュー（essayProblems.ts）

### 投入数
| 区分 | 件数 |
|---|---|
| 年度 | 12（H25〜R6 全網羅） |
| 各年度の問数 | 2 問（IPA 試験の午後II は問1+問2 から 1 問選択） |
| 合計 | **24 件**（R6-PM2-1 から H25-PM2-2 まで） |

### 字数指定の均一性
全 24 問が PM 試験 午後II の標準形式に完全準拠:
- 設問ア: `{ min: 0, max: 800 }` → 「800字以内」
- 設問イ: `{ min: 800, max: 1600 }` → 「800〜1600字」
- 設問ウ: `{ min: 600, max: 1200 }` → 「600〜1200字」

24 件 × 3 設問 = 72 行すべて同パターン。バラつきなし。

### テーマの多様性（24 件、重複なし）
IPA 公式の午後II テーマと一致を確認:
- R6: コストマネジメント / リーダーシップの選択
- R5: テーラリング / 終結時の評価
- R4: 事業環境変化への対応 / ステークホルダコミュニケーション
- R3: チーム内対立解消 / スケジュール管理
- R2: 未経験技術 / リスクマネジメント
- R1: コスト超過防止 / 助言活用による問題解決
- H30: 非機能要件と関係部門連携 / 本稼働間近の問題対応
- H29: 信頼関係構築 / 品質管理
- H28: 成果物再利用 / リスクコントロール
- H27: サプライヤ管理 / 品質評価・分析
- H26: 工数見積りとコントロール / 要員マネジメント
- H25: 情報セキュリティ確保 / トレードオフ解消

### categoryIds の妥当性スポット確認
- H25-PM2-1 情報セキュリティ → `['uncertainty', 'governance', 'project-work']` ✓
- H25-PM2-2 トレードオフ → `['integration', 'planning', 'uncertainty', 'stakeholder']` ✓
- R6-PM2-1 コスト → `['planning', 'measurement', 'uncertainty', 'stakeholder']` ✓
- R6-PM2-2 リーダーシップ → `['team', 'stakeholder', 'uncertainty']` ✓

各 essayProblem に 3〜4 カテゴリがマッピングされ、12 カテゴリから多様に選ばれている。

### 設問本文の品質
IPA 公式 PDF からのテキスト抽出が「，」（全角カンマ）で再現されている。OCR/PDF 抽出ミスは検出されず（スポット確認の範囲内）。

### PDF URL の完全性
全 24 件で `questionPdfUrl` / `answerPdfUrl` / `commentaryPdfUrl` の 3 つすべて投入済（IPA 直リンク）。

### 論述採点機能スコープ確認
[[project_scope]] の規定通り、本実装は **「インデックス + 推奨字数」のみ**。論述採点ロジックは含まれず、ユーザの自己採点（5 項目 × 5 段階）に依存する設計。スコープ通り。

## UI レビュー（4 commit）

### c9b5e6c: 設問と解答欄の近接表示（実機確認済）
**変更内容**: 旧「設問パネル（details）に 3 設問 → 別カードで解答欄 3 つ」を、「各設問のヘッダ + 設問本文 + 直下に解答欄 + 文字カウンタ」を 1 セクションにまとめる構造へ。

**評価**: 論述試験対策として明確な改善。視線移動が「設問ア → 解答欄ア」へ最小化される。実機確認で:
- 各設問カードが独立したセクション
- 設問本文の直下にテキストエリア
- カウンタが「N字 / 推奨 X〜Y字」+ ステータス（未着手 / 不足 / 上限内 / 超過）
- 設問ア・イ・ウのすべてで一貫したパターン

### 1a953d1: セルフレビュー指摘の修正
**変更内容**:
1. `EssayCharCounter`: `value === 0` のとき明示的に「未着手」色（slate-400）を表示するよう条件分岐追加
2. `src/lib/essayReview.ts` 新規作成し、`defaultSelfReview()` / `formatRecommendedChars()` を `EssaySelfReview.tsx` と `EssayTraining.tsx` から集約

**評価**: Codex のセルフレビューによる正当な品質改善。ユーティリティの集約は保守性向上、未着手表示の修正は UX 上の重要な明示性向上。

### 実機検証
- `npm run dev` 起動 → `/essay` で 24 問一覧表示 ✓
- `/essay/R6-PM2-1` で新 UI（設問+解答欄近接表示）を確認 ✓
- 600 文字入力時に「600字 / 推奨 800字以内」+「上限内」緑表示への遷移を確認 ✓
- 「未着手」初期表示（value === 0 時）も確認 ✓

## ビルド・整合性
- `npm run build` PASS
- `npm run validate-data` PASS（既存データに影響なし）

## 申し送り

特筆事項なし。F2-P5 は本レビューで完了とする。

### 残課題
- バッジ条件への組み込み（F2-P7 仕上げで対応想定）
- 各 essayProblem の commentaryPdfUrl への直接リンクが UI 上で機能しているかの追加検証は別タスクで
