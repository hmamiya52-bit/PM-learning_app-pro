# 午後Ⅰ AI自動採点 要件定義 v0.2

> 作成: 2026-09-20 / 対象 HEAD `eff68e8`
> ステータス: **ドラフト（§12 の未決事項をユーザ確認後に v1.0 へ）**
> 位置づけ: `tasks/handover_new_feature.md` §0 を埋めたもの
>
> **v0.2 の変更**: アーキテクチャを BYOK（利用者自身のAPIキー・ブラウザ直接）に確定。
> サーバサイド（Vercel Functions）は不要になり、R11（関数タイムアウト）は消滅した。

---

## 0. サマリ

午後Ⅰの解答入力後、自己採点（○/△/×）していた部分を **AI が自動採点**し、
行ごとに「何が足りないか」「どう書けば点になるか」を示し、
**失点の型を分類して次にやるべきことを提示する**機能を追加する。

**利用者自身の Anthropic API キーで、ブラウザから直接 API を呼ぶ**（BYOK）。
バックエンドは追加しない。「LocalStorage のみ・APIサーバ無し」という既存の前提を維持できる。

---

## 1. 解決したい課題

対象: 実務経験の浅いインフラエンジニア1名（PM試験受験者）。午後Ⅰの得点が最優先。

現状の演習フローは `解答入力 → 答え合わせ → 自己採点(○/△/×) → 記録` だが、
記述式の自己採点には初学者には越えられない壁が3つある。

| # | 課題 | 具体例 |
|---|---|---|
| C1 | **判定できない** | 模範解答と文言が違うとき、正解なのか失点なのか自分では判断できない。甘くも辛くもなり、点数が実力を表さない |
| C2 | **自分の解答のどこがダメか分からない** | 既存の解説は「なぜこの模範解答になるか」は書いてあるが、「**あなたの解答**に何が欠けているか」は書いていない |
| C3 | **次に何をすればいいか分からない** | 失点が「題意を外した」のか「本文の根拠を拾えなかった」のか「字数に要素を詰められなかった」のか分類されないので、対策が立たない |

**C3 が本丸。** 点数を上げるのは採点精度ではなく、失点パターンの特定と是正。
C1・C2 はそのための手段と位置づける。

---

## 2. スコープ

### やること

- 午後Ⅰ解答入力画面（`/afternoon/answers/:id/myAnswer`）での AI 自動採点
- 行（設問）ごとの判定・不足要素・**書き直し例**の提示
- 失点タイプの分類（§5.3 の分類表）
- 1回の演習に対する総合アドバイス（強み・弱み・次にやること）
- 過去の演習を横断した**弱点プロファイル**と推移（フェーズ5）
- 設定画面での APIキー管理・モデル選択・使用量表示
- AI 採点失敗時の、既存の手動自己採点へのフォールバック

### やらないこと（スコープ外）

| 項目 | 理由 |
|---|---|
| **サーバサイド処理の追加** | BYOK で不要になった。`api/` も `vercel.json` の変更も行わない |
| 午後Ⅱ（論述）の自動採点 | 引継ぎ §8 で明示的にスコープ外。本件の基盤は将来転用できるが、今回は手を出さない |
| 問題本文（IPA原文）のアプリ内収録 | 著作権。§5.1 のとおり**本文なしで採点が成立する**設計にする |
| AI 採点結果を確認なしで XP・バッジへ直結 | 誤採点が学習履歴を汚染する。§4 F4 のとおり必ず利用者確認を挟む |
| オフラインでの AI 採点 | 原理的に不可。PWA では「採点できません」と明示し手動採点に倒す |
| APIキーの端末間同期 | §6.2 のとおり、キーは同期パッケージに載せない。端末ごとに入力する |

---

## 3. 現状の整理（既存資産の棚卸し）

調査の結果、**採点に必要なデータは全問そろっている**。

| データ | ファイル | 充足状況 |
|---|---|---|
| 問題メタ | `data/afternoonProblems.ts` | 37問 |
| 公式解答例 | `data/officialAnswers.ts` | **37問すべて** |
| 配点 | `data/scoringMap.ts` | **37問すべて**（行ごとに `{correct, partial}`） |
| 公式設問文（IPA原文） | `data/afternoonQuestionTexts/` | **全12年度・296行**。`rowKey` で解答行と1:1 |
| 独自解説（行ごと） | `data/afternoonExplanations.ts` | **37問すべて**。`point` / `basis` / `reasoning` / `pitfall` |
| 詳細解説（本文セクション解説） | 同上 `detail.problemSections` | **37問すべて** |

行の対応キーは `rowKey = "${s}|${q ?? ''}|${t ?? ''}"`（例 `1|(1)|`）で全データ共通。
利用者解答のキーは `officialAnswers.answers` の**配列インデックス文字列**（`"0"`, `"1"`, …）。
この2つの変換が必要（`processRows` の `rowIndex` が橋渡しになる）。

**既存の採点フロー**（`pages/AfternoonMyAnswer.tsx`）:

```
myAnswers {rowIndex: string} → checkMode → scorings {rowIndex: 'correct'|'partial'|'wrong'}
  → calculatedScore = Σ scoringMap[id][rowIndex][judgment]
  → 全行マーク完了で「記録」活性化
  → addRecord() + applyAfternoonRecord() + addActivityEvent() + saveSavedAnswerSnapshot()
```

**AI 採点はこの `scorings` を埋める手段として差し込む。** 下流（点数計算・記録・XP・同期）は一切変えない。
これが最小の変更で最大の効果を得る接続点。

---

## 4. 機能要件

### F1. 採点の実行

- 答え合わせモードに「**AIに採点してもらう**」ボタンを置く
- APIキー未設定なら、ボタンから設定画面へ誘導する
- 未入力行があっても実行可（空欄は `wrong` 相当として扱い、その旨を返す）
- ストリーミングで受信し、進捗（採点が進んでいること）を表示する
- 二重実行を防ぐ。実行後は再採点ボタンに切り替える
- 実行のたびに `usage`（トークン数）を記録し、概算コストを画面に出す（§F7）

### F2. 行ごとのフィードバック

各解答行に対して以下を表示する。

| 項目 | 内容 | 表示 |
|---|---|---|
| 判定 | ○ / △ / × と獲得点 | 行ヘッダ |
| 確信度 | high / low。low のときは「要確認」バッジ | 判定の隣 |
| 一言講評 | 40字以内。何が良く/足りないか | 常時表示 |
| 不足要素 | 模範解答が持っていて利用者解答に無い要素（箇条書き） | 常時表示 |
| 失点タイプ | §5.3 の分類（複数可） | タグ表示 |
| **書き直し例** | **利用者自身の解答を活かして**字数条件内で満点になるよう直したもの | 折り畳み |

**書き直し例が学習効果の核。** 模範解答をもう一度見せるのではなく、
「あなたの書いた文をこう直せば点になる」を見せることで、次回の書き方が変わる。

### F3. 総合アドバイス

1回の演習全体に対して:

- **できていたこと**（1〜2点。伸ばす部分を明示）
- **崩れていたこと**（1〜3点。失点タイプの集計に基づく）
- **次にやること**（2〜3件。具体的な行動。「〇〇年度問△をもう一度」「設問を読んだら条件に線を引く」など）

### F4. 採点結果の確定（利用者確認必須）

- AI の判定は `scorings` に**プリセットされるが確定ではない**
- 利用者は任意の行の ○/△/× を上書きできる（既存 UI がそのまま使える）
- 「記録」を押した時点の `scorings` が正。AI が勝手に記録・加点することはない
- AI 判定と利用者の最終判定が食い違った行は保存しておく（§7 の `userOverrode`）→ AI 採点の信頼度を後から検証できる

> **これは安全装置であると同時に学習装置でもある。** AI の判定根拠を読んで「いや自分は合っていた」と
> 判断する行為そのものが、採点基準の内面化につながる。

### F5. 弱点プロファイル（フェーズ5）

- 過去の全演習の失点タイプを集計し、`/afternoon` に「あなたの失点の傾向」として表示
- 直近5回と全期間を比較して、改善しているタイプ／していないタイプを出す
- 特定タイプが突出しているとき、対応するノート（`/notes/afternoon-tips` 等）へ誘導する

### F6. フォールバックとエラー処理

| 状況 | 挙動 |
|---|---|
| APIキー未設定 | ボタンから設定画面へ誘導 |
| オフライン | ボタンを不活性化し「オフラインのため手動採点をお使いください」 |
| キーが無効（401） | 「APIキーが正しくありません」＋設定画面へ |
| 残高不足・上限（400/429） | 「利用上限に達しています」＋コンソールへの案内 |
| タイムアウト・通信断 | エラー文言＋再試行ボタン |
| レスポンスが壊れている | 部分的にでも読める行は反映し、残りは手動採点に倒す |

**どの失敗経路でも、既存の手動自己採点が必ず使えること。** これは必須要件。

### F7. 設定画面（BYOK固有）

- **APIキーの入力・更新・削除**。表示は末尾4桁のみ（`sk-ant-...XXXX`）
- **接続テスト**ボタン（最小のリクエストを投げて疎通と認証を確認）
- **モデル選択**（`claude-opus-5` / `claude-sonnet-5`。既定は Opus 5）
- **使用量の表示**: 累積の入出力トークンと概算コスト、採点回数。月ごとにリセット可能
- キーの取得方法（Anthropic コンソールへのリンク）と、**このアプリ専用キー＋利用上限の推奨**を明記する

---

## 5. 採点の設計

### 5.1 AI に渡す情報（問題本文なしで成立させる）

問題本文（IPA原文）はアプリに無く、著作権上も収録しない。
代わりに以下を渡す。**採点は「利用者解答 vs 模範解答＋採点基準」の照合**であり、
本文の再読解ではないので、これで成立する。

| # | 渡すもの | 出所 |
|---|---|---|
| 1 | 公式設問文（原文・字数条件込み） | `afternoonQuestionTexts` |
| 2 | 公式解答例 | `officialAnswers` |
| 3 | 配点 `{correct, partial}` | `scoringMap` |
| 4 | 採点基準（この設問が問う力点・本文の根拠・なぜその解答か・ありがちな失点） | `afternoonExplanations.rows[]` の `point`/`basis`/`reasoning`/`pitfall` |
| 5 | 問題の概要と本文セクション解説 | `afternoonExplanations.overview` / `detail.problemSections` |
| 6 | 利用者の解答（全行） | `myAnswers` |

`pitfall` は「ありがちな失点・誤答パターン」なので、**そのまま採点基準として効く**。
既存の解説データが期せずしてルーブリックになっている。

### 5.2 AI に禁止すること（プロンプトに明記）

- 模範解答と文言が違うだけで × にしない。**要素の一致で判定する**
- 逆に、もっともらしいが本文の根拠に基づかない解答を ○ にしない
- 配点を勝手に作らない。判定は ○/△/× の3値のみ（点数は `scoringMap` 由来でアプリ側が計算する）
- 書き直し例は**必ず設問の字数条件内**に収める
- 採点基準に無い独自の正解を持ち出さない

### 5.3 失点タイプの分類

午後Ⅰの失点は概ね以下に収まる。この分類が F3・F5 のアドバイスを駆動する。

| コード | 名称 | 説明 | 典型例 |
|---|---|---|---|
| `intent-miss` | 題意外し | 問われていることと違うことに答えた | 「事業面の狙い」と限定されているのに開発上の理由を書く |
| `evidence-missing` | 根拠不足 | 本文の記述に基づかず一般論・教科書的解答になった | 「適切にリスク管理する」 |
| `element-missing` | 要素不足 | 方向は合っているが必要な要素が欠けた | 「二つ挙げよ」に一つしか書かない／目的まで踏み込まない |
| `too-abstract` | 抽象度過多 | 具体性が足りず何を指すか特定できない | 「関係者と調整する」 |
| `length-violation` | 字数条件違反 | 指定字数の超過・不足 | 35字以内に要素を詰め切れない |
| `subject-mismatch` | 主語・対象ずれ | 誰が/誰に/何を、がずれている | 主体がPMなのに利用者を主語にした |
| `direction-wrong` | 方向性誤り | 根本的に別の解答 | — |

複数該当を許す（例: `intent-miss` + `too-abstract`）。

### 5.4 呼び出し方式

- **1問まるごと1リクエスト**（行ごとに分割しない）。設問間の関係を見て採点できるほうが精度が高く、レイテンシとコストも有利
- **構造化出力**（`output_config.format` のJSONスキーマ）で返させる。自由記述のパースはしない
- **ストリーミング**（`client.messages.stream()`）を使う。長いリクエストで接続が切れるのを避けるため（公式ドキュメント「Long requests」の推奨）
- `thinking: { type: 'adaptive' }`（採点は判断タスク）
- モデル: 既定 **`claude-opus-5`**、設定で `claude-sonnet-5` に切替可（§8.1）
- 公式 SDK `@anthropic-ai/sdk` を使う（生の fetch は書かない）

---

## 6. アーキテクチャ

### 6.1 構成（BYOK・ブラウザ直接）

```
ブラウザ (React PWA)
  ├─ 設定画面: 利用者が自分の Anthropic APIキーを入力
  │    → pmap:ai:apiKey（端末ローカル・同期対象外）
  │
  └─ 採点実行時:
       const { default: Anthropic } = await import('@anthropic-ai/sdk')   // 動的import
       const client = new Anthropic({
         apiKey,
         dangerouslyAllowBrowser: true,
         logLevel: 'off',        // デバッグログにボディが出るのを防ぐ
       })
       client.messages.stream({ model, ... })  →  Anthropic API へ直接
```

**サーバは無い。** `api/` ディレクトリも `vercel.json` の変更も不要。
Vercel は従来どおり静的サイトを配信するだけ。

### 6.2 `dangerouslyAllowBrowser` の妥当性

このオプションが「dangerously」と名付けられているのは、
**開発者の鍵を不特定の利用者に晒す**構図を防ぐため。公式ドキュメントは、
危険でないケースとして次を挙げている（原文要旨）:

- **Internal tools**: 信頼できる利用者だけの閉じた環境で使われるアプリ
- 短命・ローテーション可能な資格情報を使う場合

本件は**利用者が自分の鍵を自分の端末に置く**構図で、鍵が第三者に渡ることはない。
残る現実的なリスクは ①端末への物理アクセス ②アプリに XSS が入った場合 の2つに限定される。

**対策（設定画面に明記する）:**

1. **このアプリ専用の API キーを発行する**（他用途と共用しない）
2. Anthropic コンソールで**利用上限（spend limit）を設定**する → 漏洩時の被害が有限になる
3. 不審な兆候があれば**コンソールから即失効**できる
4. キーは同期パッケージに載せない／ログに出さない（`logLevel: 'off'`）／UI では末尾4桁のみ表示
5. エラートーストやコンソール出力にキーが混入しないことを実装時に確認する

### 6.3 SDK の読み込み方

- `@anthropic-ai/sdk` を `dependencies` に追加する
- **採点実行時に動的 import する**（`await import('@anthropic-ai/sdk')`）
  → 初期バンドルと PWA のプリキャッシュを太らせない。既存のルート `React.lazy` と同じ方針
- SDK を読み込まない限り、AI 機能はアプリの起動時間に一切影響しない

### 6.4 方式の比較（なぜ BYOK か）

| 方式 | 長所 | 短所 | 採否 |
|---|---|---|---|
| **BYOK（採用）** | バックエンド不要。既存の「APIサーバ無し」前提を維持。関数タイムアウトの制約が無い。費用の主体が明確 | キーが端末の LocalStorage に載る（§6.2 で緩和） | **採用** |
| Vercel Functions（提供者キー） | キーがブラウザに出ない | 公開エンドポイントが有料APIのプロキシになる。認証がクライアント側だけなので守りにくい。関数の実行時間上限 | 不採用 |

### 6.5 P1 で必ず実測すること

1. **ブラウザから CORS が通るか**（公式にブラウザ対応と明記されているが、実測で確認する）
2. 採点1回の**所要時間**
3. 採点1回の **`usage`（入出力トークン）と実コスト**

---

## 7. データ設計

### 7.1 採点結果の型（新規 `src/lib/afternoonAiGrading.ts`）

```ts
export type AfternoonErrorType =
  | 'intent-miss' | 'evidence-missing' | 'element-missing'
  | 'too-abstract' | 'length-violation' | 'subject-mismatch' | 'direction-wrong'

export interface AfternoonAiRowResult {
  rowKey: string                 // '1|(1)|'
  rowIndex: number               // scorings のキーと一致
  judgment: AfternoonMarking     // 'correct' | 'partial' | 'wrong'（既存型を再利用）
  confidence: 'high' | 'low'
  summary: string                // 40字以内
  missing: string[]              // 不足要素
  errorTypes: AfternoonErrorType[]
  rewrite?: string               // 書き直し例（字数条件内）
  userOverrode?: boolean         // 利用者が最終的に別判定にしたか（記録時に確定）
}

export interface AfternoonAiGrading {
  version: 1
  model: string                  // 'claude-opus-5'
  gradedAt: string               // ISO
  problemId: string
  rows: AfternoonAiRowResult[]
  proposedScore: number          // AI 判定のみで計算した点（参考値）
  overall: {
    strengths: string[]
    weaknesses: string[]
    nextActions: string[]
  }
}
```

### 7.2 採点結果の永続化

既存の `AfternoonSavedAnswerSnapshot`（`pmap:savedAnswers:<recordId>`）に
**任意フィールドとして追加する**。新しいキーは作らない。

```ts
export interface AfternoonSavedAnswerSnapshot {
  version: 1
  answers: AfternoonAnswerMap
  scorings: AfternoonScoringMap
  score?: number
  savedAt?: string
  aiGrading?: AfternoonAiGrading   // ★追加
}
```

理由:
- 記録の削除と一緒に消える（孤児データが出ない）。既存の `clearAllSavedAnswerSnapshots()` もそのまま効く
- **同期に自動的に乗る**（`LocalSyncState.savedAnswerSnapshots` が既に同期対象）
- 過去データは `aiGrading` 無しで正常に読める（`normalizeSavedAnswerSnapshot` に正規化を足すだけ）

### 7.3 設定の永続化（新規 `src/lib/aiSettings.ts`）

`preferences.ts` の「端末ローカル・同期対象外」パターンを踏襲する。

| キー | 内容 | 同期 |
|---|---|---|
| `pmap:ai:apiKey` | 利用者の Anthropic APIキー | **しない（絶対）** |
| `pmap:ai:model` | 使用モデル | しない |
| `pmap:ai:enabled` | AI採点機能の ON/OFF | しない |
| `pmap:ai:usage` | 累積トークン・概算コスト・採点回数 | しない |

- **`pmap:ai:apiKey` が同期パッケージに混入しないこと**を実装時に必ず検証する
  （`readLocalSyncState()` は明示列挙なので自動混入はしないが、テストで固定する）
- 学習データのリセット（`resetModeData` / `resetAllStudyData`）では**キーを消さない**。
  キーの削除は設定画面の専用ボタンで行う

### 7.4 同期の扱い

| 対象 | 同期 | 理由 |
|---|---|---|
| `aiGrading`（採点結果） | **する** | 端末をまたいで弱点プロファイル（F5）が連続しないと意味がない |
| APIキー・モデル・使用量 | **しない** | 秘密情報／端末ごとの設定。QRコードに秘密を載せない |

`savedAnswerSnapshots` 経由なので **`SYNC_SCHEMA_VERSION`（現在2）の引き上げは不要**
（任意フィールドの追加で、旧バージョンは無視するだけ）。
ただし実装時に、旧端末→新端末のマージで `aiGrading` が落ちないことを検証すること。

### 7.5 容量

- 1回分の `aiGrading` ≈ 3〜4KB（日本語 JSON）
- リスクレジスタ R4 で LocalStorage は既に 3〜4MB / 5MB を使用見込み
- 50回分で約 200KB。許容範囲だが、**`aiGrading` を持つ記録数の上限（例: 直近60件）を設け、
  古いものから `aiGrading` のみ削除する**方針を実装に含める（記録本体は消さない）

---

## 8. 非機能要件

| 項目 | 要件 |
|---|---|
| 応答時間 | 目標 60秒以内。ストリーミングで進捗を見せ、待ち時間を体感的に短くする |
| コスト | §8.1。利用者が自分のコンソールで上限を設定する。アプリ側でも使用量を表示する |
| 可用性 | AI が使えなくても演習・手動採点・記録は全機能が動くこと |
| 機密 | APIキーをリポジトリ・ログ・同期パッケージ・エラー表示に含めない |
| バンドル | SDK は動的 import。初期ロードと PWA プリキャッシュのサイズを増やさない |
| 対応幅 | モバイル375px と通常幅の両方。フィードバックが長いので折り畳みを活用 |
| 既存への影響 | `tracker` / `gamification` / `activityLog` / `sync` のスキーマを変えない |

### 8.1 コスト概算

1回の採点あたり（入力 約8,000トークン / 出力＋思考 約7,000トークン と仮定）:

| モデル | 単価(入/出 per 1M) | 1回あたり | 37問×3周=111回 |
|---|---|---|---|
| Claude Opus 5（既定） | $5 / $25 | 約 $0.21（≒30円） | 約 $23（≒3,500円） |
| Claude Sonnet 5 | $2 / $10 | 約 $0.09（≒13円） | 約 $10（≒1,500円） |

**概算であり、P1 で `usage` を実測して確定させる。**
BYOK なので費用は利用者負担。設定でモデルを切り替えられるようにし、
既定は判定品質を優先して Opus 5 とする。

---

## 9. リスク

既存 `memory/risks.md` に追加すべきもの。

| ID | リスク | 影響 | 対応 |
|---|---|---|---|
| **R9** | **AI の誤採点による誤学習** | 間違った基準を身につける。R2（コンテンツ品質）の新しい形 | 判定根拠を必ず併記／利用者確認を必須化（F4）／確信度 low を明示／`userOverrode` を蓄積して精度を後追い検証 |
| **R10** | **APIキーが端末の LocalStorage に平文で残る** | キー漏洩→金銭被害 | §6.2 の5点（専用キー・利用上限・即失効・同期しない/ログに出さない・混入検査） |
| **R11** | ~~Vercel Functions のタイムアウト超過~~ | — | **BYOK 採用により消滅** |
| **R11'** | 長時間リクエスト中の接続断 | 採点が失敗する | ストリーミングを使う（公式推奨）／再試行ボタン／手動採点へのフォールバック |
| **R12** | IPA原文（設問文・解答例）を外部APIへ送信する | 著作権・運用上の判断 | 個人の学習目的の範囲だが、判断として明記。§12 Q4 で確認 |
| **R13** | SDK 追加によるバンドル肥大 | 初期ロード・PWA体験の劣化 | 動的 import（§6.3）／ビルド後にバンドルサイズを確認 |
| R4（既存） | LocalStorage 5MB 超過 | 学習履歴喪失 | §7.5 の上限・剪定方針を実装に含める |

---

## 10. 実装フェーズ

縦切りで、各フェーズ終わりに動くものが残るように進める。

| P | 内容 | 完了の形 |
|---|---|---|
| **P1** | **技術検証**: ブラウザから SDK 直接呼び出しで1問採点を通す。CORS・所要時間・`usage` を実測 | §6.5 の3点が判明する |
| P2 | 採点材料の組み立て（§5.1）＋プロンプト＋構造化出力スキーマ。全37問で形式が成立することを確認 | 全問で採点結果が得られる |
| P3 | 設定画面（F7）: キー入力・接続テスト・モデル選択・使用量表示 | キーを入れて疎通確認できる |
| P4 | 採点UI: ボタン・ストリーミング進捗・行ごとのフィードバック・書き直し例・`scorings` へのプリセット | 画面上で採点→確認→記録が通る |
| P5 | 総合アドバイス（F3）＋失点タイプのタグ表示 | 1回の演習に対する講評が出る |
| P6 | 弱点プロファイル（F5）。`/afternoon` に傾向と推移を表示 | 履歴横断のアドバイスが出る |
| P7 | エラー処理の総仕上げ、容量剪定、同期検証 | §11 を全部通せる |

**P1 が不発なら設計を見直す**ので、P1 を独立させている。
P3 を P4 より先に置いているのは、キーが無いと採点UIの動作確認ができないため。

---

## 11. 完了の判定条件

引継ぎ §7 の手順に加えて、本機能固有の条件。

1. `npx tsc --noEmit -p tsconfig.app.json` → エラー 0
2. `npm run validate-data` → `[OK] 全データの整合性確認完了`
3. `npm run lint` → src 配下 error 0
4. `npm run build` → 成功
5. Browser pane で 375px と通常幅の両方を実機確認。コンソールエラー 0
6. **AI採点の成功経路**: 採点 → 行ごとのフィードバック表示 → `scorings` プリセット → 上書き可 → 記録 → XP加算 → 履歴に残る
7. **AI採点の失敗経路**: キー未設定／オフライン／無効キー／API エラー のそれぞれで、**手動自己採点が従来どおり完走できる**
8. 既存の記録（`aiGrading` 無し）を開いても壊れない
9. 端末間同期で `aiGrading` が失われず、**`pmap:ai:apiKey` が同期パッケージに含まれない**
10. `git grep` で APIキーがリポジトリに無いこと。ビルド成果物・コンソールログにも出ないこと
11. 動的 import が効いていること（AI機能を使わない限り SDK が読み込まれない）

---

## 12. 未決事項（着手前にユーザ確認）

| # | 論点 | 推奨 | 状態 |
|---|---|---|---|
| ~~Q1~~ | アーキテクチャ | **BYOK（利用者キー・ブラウザ直接）** | **確定（v0.2）** |
| Q2 | 既定モデルは Claude Opus 5 でよいか（設定で Sonnet 5 に切替可能にする） | **Opus 5 既定・切替可** | 未 |
| Q3 | AI判定を利用者が確認してから記録する方式で良いか（AIが自動で記録・加点しない） | **確認必須** | 未 |
| Q4 | IPA の設問文・解答例を外部APIへ送信することの是非（R12） | 個人学習目的として許容と判断 | 未 |
| Q5 | 弱点プロファイル（F5・履歴横断）まで作るか、1回ごとの講評（F5手前）で止めるか | **F5 まで作る**（C3 がこの機能の本丸のため） | 未 |

---

以上。
